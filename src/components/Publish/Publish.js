import { View, Text } from "react-native";
import React from "react";

const Publish = () => {
  return (
    <View
      style={{
        backgroundColor: "#F6E1DC",
        width: "100%",
        height: "100%",
        // overflow: "scroll",
        marginTop: 5,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <Text style={{ fontSize: 15, margin: 40 }}>
        Sorry, We are still Working on this Feature.{" "}
      </Text>
    </View>
  );
};

export default Publish;
